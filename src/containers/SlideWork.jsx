import { useState, useRef } from "react";
import { Clock, ClockDisplay } from "../components/Clock";

export const SlideWork = () => {
  return (
    <div>
      <ClockDisplay></ClockDisplay>
    </div>
  );
};
